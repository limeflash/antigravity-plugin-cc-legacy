---
description: Show all /agy:* commands, supported --model aliases, and canonical model names
allowed-tools: Bash(bash:*)
---

Run the wrapper's help branch and return its stdout verbatim. Do not
paraphrase, reorder, or summarize — the wrapper is the authoritative source
for the command list, model aliases, and canonical model names.

```
bash "${CLAUDE_PLUGIN_ROOT}/scripts/agy-run.sh" help
```
